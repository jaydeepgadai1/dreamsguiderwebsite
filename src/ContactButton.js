// ContactButton.js
import React from 'react';

function ContactButton({ onContactNowClick }) {
  return (
    <button onClick={onContactNowClick}>Contact Now</button>
  );
}

export default ContactButton;
